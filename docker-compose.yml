version: '3.8'

services:
  writerside-builder:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/app
    command: /opt/builder/bin/idea.sh build --input-dir=/app --output-dir=/app/output
